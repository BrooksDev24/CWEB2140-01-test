version: "3.8"
services:
  snaketestapp:
    image: schbros/testauto:latest
    build: .
    contaianer_name: testauto
    ports: 
      - "80:3000"
    enviroment:
      - PORT=3000
    volumes:
      - ./:/app
    command: ["node", "app.js"]
    
